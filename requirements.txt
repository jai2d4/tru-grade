# Web service dependencies.
#
# Deliberately free of torch / ultralytics / easyocr / opencv. Those are
# the analysis worker's concern (requirements-worker.txt) — measured at
# 499 MB for torch alone and 539 MB with ultralytics, against a 512 MB
# web instance. Keeping them out is what stops the web service from being
# one import away from an OOM kill, and it keeps the deployed image small.
#
# Pinned to the exact versions the test suite was verified against: a
# floating >= meant a fresh `pip install` could resolve different versions
# with no code change and no record of what moved.
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pydantic==2.13.5
pydantic-settings==2.15.0
google-genai==2.23.0
asyncpg==0.31.0
sqlalchemy[asyncio]==2.0.52
numpy==2.4.6
yt-dlp==2026.8.19
