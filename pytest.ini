[pytest]
pythonpath = .
# livephoria/ is a self-contained app with its own `app` package and its own
# pytest.ini. Collecting it from here would clash with this project's `app`.
# Run it with: cd livephoria && pytest
norecursedirs = livephoria
